# Game Assets API – MongoDB + FastAPI Project
## Project Setup

pip freeze > requirements.txt
This command will create a `requirements.txt` file in your current directory with a list
of all installed packages and their versions.
